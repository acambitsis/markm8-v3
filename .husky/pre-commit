#!/bin/sh
# Disable concurrent to run `check-types` after ESLint in lint-staged
# Use bunx for consistency with project's Bun-first approach
cd "$(dirname "$0")/.." && bunx --bun lint-staged --concurrent false
